<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ========================================= -->
    <!-- DARK GRAPHITE THEME -->
    <!-- ========================================= -->

    <!-- BACKGROUNDS -->

    <Color x:Key="BackgroundPrimaryColor">
        #161616
    </Color>

    <Color x:Key="BackgroundSecondaryColor">
        #1E1E1E
    </Color>

    <Color x:Key="SurfaceColor">
        #252525
    </Color>

    <!-- INTERACTION -->

    <Color x:Key="HoverColor">
        #2D2D2D
    </Color>

    <Color x:Key="ActiveColor">
        #323232
    </Color>

    <!-- BORDERS -->

    <Color x:Key="BorderColor">
        #3A3A3A
    </Color>

    <!-- TEXT -->

    <Color x:Key="TextPrimaryColor">
        #F2F2F2
    </Color>

    <Color x:Key="TextSecondaryColor">
        #B8B8B8
    </Color>

    <!-- ACCENT -->

    <Color x:Key="AccentColor">
        #5E81AC
    </Color>

    <!-- ========================================= -->
    <!-- BRUSHES -->
    <!-- ========================================= -->

    <SolidColorBrush
        x:Key="BackgroundPrimaryBrush"
        Color="{StaticResource BackgroundPrimaryColor}" />

    <SolidColorBrush
        x:Key="BackgroundSecondaryBrush"
        Color="{StaticResource BackgroundSecondaryColor}" />

    <SolidColorBrush
        x:Key="SurfaceBrush"
        Color="{StaticResource SurfaceColor}" />

    <SolidColorBrush
        x:Key="HoverBrush"
        Color="{StaticResource HoverColor}" />

    <SolidColorBrush
        x:Key="ActiveBrush"
        Color="{StaticResource ActiveColor}" />

    <SolidColorBrush
        x:Key="BorderBrush"
        Color="{StaticResource BorderColor}" />

    <SolidColorBrush
        x:Key="TextPrimaryBrush"
        Color="{StaticResource TextPrimaryColor}" />

    <SolidColorBrush
        x:Key="TextSecondaryBrush"
        Color="{StaticResource TextSecondaryColor}" />

    <SolidColorBrush
        x:Key="AccentBrush"
        Color="{StaticResource AccentColor}" />

</ResourceDictionary>
