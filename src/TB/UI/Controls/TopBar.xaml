<UserControl x:Class="TB.UI.Controls.TabStrip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource TBBackgroundBrush}">

    <DockPanel Background="{StaticResource TBBackgroundBrush}"
               Margin="{StaticResource TBToolbarMargin}">

        <Button Width="40"
                Style="{StaticResource TBButtonStyle}"
                Content="+"
                Command="{Binding AddTabCommand}" />

        <ListBox Margin="8,0,0,0"
                 Height="28"
                 BorderThickness="0"
                 Background="{StaticResource TBBackgroundBrush}"
                 Foreground="{StaticResource TBTextBrush}"
                 ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                 ItemsSource="{Binding Tabs}"
                 SelectedItem="{Binding ActiveTab, Mode=TwoWay}">

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemContainerStyle>

                <Style TargetType="{x:Type ListBoxItem}">

                    <Setter Property="Background"
                            Value="{StaticResource TBSurfaceBrush}" />

                    <Setter Property="BorderBrush"
                            Value="{StaticResource TBBorderBrush}" />

                    <Setter Property="BorderThickness"
                            Value="1" />

                    <Setter Property="Padding"
                            Value="0" />

                    <Setter Property="Margin"
                            Value="0" />

                    <Setter Property="Template">

                        <Setter.Value>

                            <ControlTemplate TargetType="{x:Type ListBoxItem}">

                                <Border x:Name="TabBorder"
                                        Margin="{StaticResource TBTabMargin}"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">

                                    <ContentPresenter />

                                </Border>

                                <ControlTemplate.Triggers>

                                    <Trigger Property="IsSelected"
                                             Value="True">

                                        <Setter TargetName="TabBorder"
                                                Property="Background"
                                                Value="{StaticResource TBSurfaceHoverBrush}" />

                                    </Trigger>

                                </ControlTemplate.Triggers>

                            </ControlTemplate>

                        </Setter.Value>

                    </Setter>

                </Style>

            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>

                <DataTemplate>

                    <StackPanel Orientation="Horizontal"
                                Margin="0"
                                VerticalAlignment="Center">

                        <TextBlock VerticalAlignment="Center"
                                   Foreground="{StaticResource TBTextBrush}"
                                   Text="{Binding Title}" />

                        <Button Width="20"
                                Margin="{StaticResource TBTabCloseButtonMargin}"
                                Style="{StaticResource TBButtonStyle}"
                                Content="×"
                                Command="{Binding DataContext.CloseTabCommand,
                                                  RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}" />

                    </StackPanel>

                </DataTemplate>

            </ListBox.ItemTemplate>

        </ListBox>

    </DockPanel>

</UserControl>
