<Window
    x:Class="TradingBrowser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid Background="#13111c">
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="#1e1b29" Padding="16,0,16,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                <TextBlock Text="X" Foreground="#ff9f43" FontSize="22" FontWeight="Bold" FontFamily="Segoe UI Symbol"/>
                <TextBlock Text=" Dext" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="4,0,0,0"/>
            </StackPanel>

            <controls:TabView x:Name="MainBrowserTabs" Grid.Column="1" VerticalAlignment="Bottom" Margin="0,0,8,0" Background="Transparent">
                <controls:TabView.TabItems>
                    <controls:TabViewItem Header="Workspace: Qwen Studio" IsSelected="True" Height="36"/>
                </controls:TabView.TabItems>
            </controls:TabView>

            <Grid Grid.Column="2" MaxWidth="520" Height="38" VerticalAlignment="Center">
                <AutoSuggestBox x:Name="Omnibox" 
                                PlaceholderText="Advanced: tradingview lightweight charts multi-chart setup" 
                                QueryIcon="Find" 
                                Background="#2d293d" 
                                Foreground="White" 
                                BorderThickness="0"
                                VerticalAlignment="Center"
                                CornerRadius="19"
                                Padding="16,4,16,4"/>
            </Grid>
        </Grid>

        <Grid x:Name="TabContentDisplayGrid" Grid.Row="1" Background="#13111c" />
    </Grid>
</Window>
