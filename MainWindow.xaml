<Window
    x:Class="MyPortableBrowser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="Portable Chrome Lite">

    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <!-- Top Navigation Bar (Chrome style) -->
      <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#F3F3F3" Padding="10,5,10,5" Spacing="8">
        <Button x:Name="BackButton" Content="" FontFamily="Segoe MDL2 Assets" Click="BackButton_Click"/>
        <Button x:Name="ForwardButton" Content="" FontFamily="Segoe MDL2 Assets" Click="ForwardButton_Click"/>
        <Button x:Name="ReloadButton" Content="" FontFamily="Segoe MDL2 Assets" Click="ReloadButton_Click"/>
        
        <TextBox x:Name="UrlBar" Width="600" PlaceholderText="Search Google or type a URL..." KeyDown="UrlBar_KeyDown"/>
        <Button x:Name="GoButton" Content="Go" Click="GoButton_Click" Style="{ThemeResource AccentButtonStyle}"/>
      </StackPanel>

      <!-- Browser Core -->
      <controls:WebView2 x:Name="WebView" Grid.Row="1" Source="https://www.google.com"/>
    </Grid>
</Window>
