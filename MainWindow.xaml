<Window
    x:Class="TradingBrowser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid Background="#121212">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Dynamic Header Control Strip Wrapper -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="#1C1C1C" Height="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation Controls Block -->
            <controls:TabView x:Name="MainBrowserTabs" Grid.Column="0" VerticalAlignment="Bottom" Background="Transparent">
                <controls:TabView.TabItems>
                    <controls:TabViewItem Header="TradingView Workspace" IsSelected="True" />
                </controls:TabView.TabItems>
            </controls:TabView>

            <!-- Dynamic Direct Input Omnibox -->
            <AutoSuggestBox x:Name="Omnibox" Grid.Column="1" Height="32" Margin="10,0,10,0" VerticalAlignment="Center" PlaceholderText="Enter Ticker or Search URL..." QueryIcon="Find" Background="#2A2A2A" Foreground="White" BorderThickness="0"/>
        </Grid>

        <!-- Standard Empty Canvas Root Container Window -->
        <Grid x:Name="TabContentDisplayGrid" Grid.Row="1" Background="#121212" />
    </Grid>
</Window>
