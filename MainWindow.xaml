<Window
    x:Class="TradingBrowser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid Background="#1C1B1F">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="44"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="#1F1F1F" Padding="0,0,140,0">
            <controls:TabView x:Name="MainBrowserTabs" 
                              VerticalAlignment="Bottom" 
                              UpdateLayoutTrigger="Default"
                              Background="Transparent"/>
        </Grid>

        <Grid Grid.Row="1" Background="#2E2E2E" Padding="8,0,8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Spacing="4" Margin="0,0,8,0">
                <Button x:Name="BackButton" Content="" FontFamily="Segoe MDL2 Assets" Background="Transparent" BorderThickness="0" Foreground="White" Width="28" Height="28" CornerRadius="14"/>
                <Button x:Name="ForwardButton" Content="" FontFamily="Segoe MDL2 Assets" Background="Transparent" BorderThickness="0" Foreground="White" Width="28" Height="28" CornerRadius="14"/>
                <Button x:Name="RefreshButton" Content="" FontFamily="Segoe MDL2 Assets" Background="Transparent" BorderThickness="0" Foreground="White" Width="28" Height="28" CornerRadius="14"/>
            </StackPanel>

            <AutoSuggestBox x:Name="Omnibox" 
                            Grid.Column="1" 
                            VerticalAlignment="Center" 
                            PlaceholderText="Search Google or type a URL" 
                            QueryIcon="Find" 
                            Background="#1F1F1F" 
                            Foreground="White" 
                            BorderThickness="0"
                            CornerRadius="16"
                            Padding="12,3,12,3"/>
        </Grid>

        <Grid x:Name="TabContentDisplayGrid" Grid.Row="2" Background="#1C1B1F" />
    </Grid>
</Window>
